namespace GameData.Resources.Dialog.Branches;

/// <summary>
/// <c>globalKey == 0</c>. Always matches — the "default" / fall-through edge.
/// By far the most common branch in the data.
/// </summary>
public class DefaultBranch : DialogBranchBase;
