namespace GameData.Resources.Animation.Commands;

public class DisposeCurrentBitmap : FrameCommand {
    public override string ToString() {
        return $"{nameof(DisposeCurrentBitmap)}();";
    }
}